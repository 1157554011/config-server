server:
  port: 3344

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/damonYuanlai/springcloud-config-server.git #仓库地址
